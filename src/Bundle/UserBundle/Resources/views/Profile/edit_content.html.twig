{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('user_edit_profile'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
<div>
    <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
</div>
{{ form_end(form) }}
